**[Focus:: App]**
**Updates and Iteration:**

Top bar: 
- Add the action button group (view toggle, edit, options, pin, and Save button) to the top-left area above the  code editor, aligning it next to the back arrow/file navigation icon.

- Relocate the file status metadata (file icon, blog/post1.md, Knowledge Base tag, and "All changes saved") to the top-right section where the toolbar buttons were originally located.

- Create a tab system that will show opened files, if user didn't add a new tab via ctrl+n then the system will open every files on the same tab..

Panels: 
- Implement a draggable divider (resizer handle) between each pane/panel so the user can adjust pane widths dynamically.

- Add round corners to each panel and some small gaps in between them..

Study board: 
-  Remove note editor interface, the entire study board will be the editor's interface without having Note title dialog  and preview, cancel and create button.. please apply-obsidian's structure system in the Study board..

Sources:

- Sources content will be previewed on sources panel only 
- Sources should support multiple files for CRUD operations 
- Sources can be edited but original versions will stay untouched. 
- Sources will be kept inside a sources icon in left navbar.. 




---

**Changelogs: CHANGELOG.md**